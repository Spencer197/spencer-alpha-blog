<!DOCTYPE html>
<html>
<head>
<title>Welcome To My Webpage</title>
</head>
<body>
<h1 align="center">Welcome to <%= @user.username %>'s page</h1><%# Welcomes user to his/her show page by user's name. %>
<div class="row center"><%#Had to move 'center' from class="col-md-4 col-md-offset-4" to class="row" to center align gravatar image.%>
  <div class="col-md-4 col-md-offset-4">
    <%= gravatar_for @user, size: 150 %><%#Specifies size of gravatar image.%>
  </div>
</div>
<h4 align="center"><%= @user.username %>'s articles</h4>
<div class="center">
  <%= will_paginate @user_articles %>
</div>
<%= render 'articles/article', obj: @user_articles %><%#Renders the _article partial to display a selected article.%>
<div class="center">
  <%= will_paginate @user_articles %>
</div>
</body>
</html>